[project]
name = "gitlint-rules-tests"
version = "0.0.0"
description = "Tests for custom gitlint rules used by this repo."
requires-python = ">=3.10"
dependencies = [
    "gitlint>=0.19.1",
    "pytest>=8.3.0",
]

[tool.pytest.ini_options]
testpaths = [".gitlint-rules/tests"]
addopts = "-ra -q"
